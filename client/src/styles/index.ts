import { createStitches } from '@stitches/react'

import { theme } from './theme'

export const {
  config,
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  createTheme
} = createStitches({
  theme
})
